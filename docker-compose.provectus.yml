services:
  kafka-ui:
    container_name: kafka-ui
    image: provectuslabs/kafka-ui:latest
    ports:
      - 8082:8080
    environment:
      DYNAMIC_CONFIG_ENABLED: 'true'
    volumes:
      - $DEVBOX_CONFIG_DIR/provectus/config.yml:/etc/kafkaui/dynamic_config.yaml

